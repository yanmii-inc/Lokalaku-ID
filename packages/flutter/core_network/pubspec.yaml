name: lokalaku_core_network
description: >
  HTTP client, auth interceptor, error models, and retry logic.
  Pure Dart — no Flutter SDK dependency.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  dio: ^5.6.0
  lokalaku_domain:
    path: ../domain

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
