name: lokalaku_utils
description: >
  Pure Dart utility library — Rupiah formatter, date/time helpers,
  input validators, and common extensions.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  intl: ^0.19.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
